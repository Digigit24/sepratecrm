// Registers jest-dom's DOM matchers on vitest's `expect`.
// The matching TYPE declarations live in `src/types/testing.d.ts`, because
// tsconfig.app.json only includes `src`.
import '@testing-library/jest-dom/vitest';
