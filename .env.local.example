# Copy to .env.local. Relative paths are proxied by vite.config.ts to the
# local backends, which keeps remote (Tailscale) access working.
VITE_AUTH_BASE_URL=/auth-api/api
VITE_CRM_BASE_URL=/crm-api/api
